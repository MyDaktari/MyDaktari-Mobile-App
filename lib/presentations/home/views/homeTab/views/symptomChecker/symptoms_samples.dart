import 'package:flutter/material.dart';

class SymptomSamples extends StatelessWidget {
  const SymptomSamples({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
